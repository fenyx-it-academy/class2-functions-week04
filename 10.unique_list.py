def unique_list(list1):

    return list(set(list1))  # Once we convert our list into a set, we have unique set of elements.


print(unique_list([1, 2, 3, 3, 3, 3, 4, 5, 5]))  # We convert again our set into a list to get the demanded output.